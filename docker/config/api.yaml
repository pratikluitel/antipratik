server:
  host: "0.0.0.0"
  port: 8080

db:
  path: "/data/antipratik.db"

site_domain: "https://localhost"      # public base URL used for email links and image URLs; override via ANTIPRATIK_SITE_DOMAIN

admin_email: "contact@antipratik.com"           # contact form recipient; override via ANTIPRATIK_ADMIN_EMAIL
admin_password: "admin"

static:
  dir: ""

storage:
  backend: "local"        # "local" or "r2"
  local_dir: "/data/uploads"
  r2:
    endpoint: "https://fb28ab9f2289a1ce960eb6a0004dc73e.r2.cloudflarestorage.com"
    bucket: "antipratik"
    access_key_id: "ff1ebe9bc48dc4fdab31fd86f3c41e39"
    secret_access_key: "8882f343b692c4390bf38b40a3316c3b93aa1b331c7f7aa3e9941cad38611e0b"

logging:
  level: "info"         # debug | info | warn | error

broadcaster:
  resend:
    api_key: ""           # override via ANTIPRATIK_RESEND_API_KEY (secret)
    host: "smtp.resend.com"
    port: 465
    from_email: "newsletter@antipratik.com"        # e.g. "newsletter@antipratik.com"
    from_name: "AntiPratik DEV"         # e.g. "Pratik"
