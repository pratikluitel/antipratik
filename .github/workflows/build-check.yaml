name: Build Check

on:
  push:
    paths:
      - 'antipratik-ui/**'

jobs:
  build-check:
    name: Build antipratik-ui
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: antipratik-ui/package-lock.json

      - name: Install dependencies
        working-directory: antipratik-ui
        run: npm ci

      - name: Build
        working-directory: antipratik-ui
        run: npm run build
