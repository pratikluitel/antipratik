server:
  host: "0.0.0.0"
  port: 8080

db:
  path: "/data/antipratik-dev.db"

admin_password: "dev-default"

static:
  dir: ""

storage:
  backend: "r2"
  r2:
    bucket: "antipratik-dev"
    endpoint: ""   
    access_key_id: ""
    secret_access_key: ""

logging:
  level: "debug"
