server:
  host: "0.0.0.0"
  port: 8080

db:
  path: "/data/antipratik-dev.db"

site_domain: "https://dev.antipratik.com"
admin_email: "contact@antipratik.com"
admin_password: "dev-default"

static:
  dir: ""

storage:
  backend: "r2"
  r2:
    bucket: "antipratik-dev"
    endpoint: ""   
    access_key_id: ""
    secret_access_key: ""

logging:
  level: "debug"

broadcaster:
  resend:
    api_key: ""           # override via ANTIPRATIK_RESEND_API_KEY (secret)
    host: "smtp.resend.com"
    port: 465
    from_email: "newsletter@antipratik.com"        # e.g. "newsletter@antipratik.com"
    from_name: "AntiPratik DEV"         # e.g. "Pratik"

