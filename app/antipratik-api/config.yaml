server:
  host: "0.0.0.0"
  port: 8080

db:
  path: "./data/antipratik.db"

static:
  dir: "../antipratik-ui/out"

storage:
  backend: "local"        # "local" or "r2"
  local_dir: "./data/uploads"
  r2:
    endpoint: ""
    bucket: "antipratik"
    access_key_id: ""
    secret_access_key: ""
